/* IMPORT */
import isFinite from './is_finite.js';
import isNumber from './is_number.js';
/* MAIN */
const isFloat = (value) => {
    return isNumber(value) && isFinite(value) && Math.floor(value) !== value;
};
/* EXPORT */
export default isFloat;
