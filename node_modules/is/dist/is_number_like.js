/* IMPORT */
import isNaN from './is_nan.js';
import isFinite from './is_finite.js';
/* MAIN */
const isNumberLike = (value) => {
    return !isNaN(parseFloat(String(value))) && isFinite(Number(value));
};
/* EXPORT */
export default isNumberLike;
